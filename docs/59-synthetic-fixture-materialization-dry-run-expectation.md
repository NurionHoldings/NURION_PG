# 기능 #059 — 합성 fixture 물질화 Dry-run expectation 초안

기능 #058의 PASS 심사에서 scenario·contract·rollback digest와 관찰값·결과가 없는
expectation descriptor만 고정한다. 최대 상태는
`SYNTHETIC_FIXTURE_MATERIALIZATION_DRY_RUN_EXPECTATION_DRAFTED`이며 별도 심사를 요구한다.

Fixture 물질화·filesystem 기록·Dry-run·활성화·rollback·결제·외부 API·병합·배포는 수행하지 않는다.
